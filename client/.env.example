# Copy to .env and set this to your backend's URL
# While testing locally, the default (http://localhost:4000) already works, so this file is optional locally.
# When you deploy the backend for real, set this to that live URL, e.g:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=http://localhost:4000
